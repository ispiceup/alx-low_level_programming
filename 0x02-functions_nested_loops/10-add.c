#include "main.h"
#include <stdio.h>

/**
 * main - Entry adds two integers and returns the result.
 * @a first integer
 * @b second integer
 * Return: Sum of a and b
 *
 */

int add(int a, int b)
{
	return (a + b);
}
